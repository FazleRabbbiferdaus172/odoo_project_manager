[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "odoo_project_manager"
version = "1.0.0"
dependencies = [
    "jinja2"
]

[project.scripts]
odopm = "odoo_project_manager.app:cli"

[tool.pyright]
extraPaths = ["src"]
include = ["src"]
